import {
  Section,
  SectionTitle,
  SectionText,
} from "../../styles/GlobalComponents";

const Projects = () => {
  return (
    <Section>
      <SectionTitle>Projects</SectionTitle>
    </Section>
  );
};

export default Projects;
